version: "3.9"

services:
  model:
    build: ./services/model
    container_name: model
    ports:
      - "8001:8001"

  api:
    build: ./services/api
    container_name: api
    ports:
      - "8000:8000"
    depends_on:
      - model
